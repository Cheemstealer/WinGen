WinGen is designed to give you an easy way to generate Windows forensic artefacts.

Use of a virtual machine is recommended for the running of WinGen.

WinGen must be run through the command line as an administrator to function as intended.
